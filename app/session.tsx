import React, { FC } from 'react'
import App from '../App'

type Props = {}

/** Route to the kanjiken exercise session component */
const SessionRoute: FC<Props> = ({}) => <App />

export default SessionRoute
